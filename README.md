# D3D11EngineInterface

Shared header-only interface definitions used by multiple D3D11-based DLL modules.
This repository contains common rendering contracts such as engine, context, layer, overlay, and UI-related interfaces.
